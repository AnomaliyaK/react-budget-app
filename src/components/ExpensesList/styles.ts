import styled from 'styled-components';

export const StyledExpensesList = styled.ul`
  display: grid;
  width: 100%;
  max-width: 768px;
  max-height: 200px;
  overflow-y: scroll;
  scroll-behavior: smooth;
`;
